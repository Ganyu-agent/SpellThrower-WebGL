# 덱 빌딩 UI 개선 — 아이디어

작성 2026-08-20 / 브랜치 `fix/pixel-font-crisp` 기준 코드 조사

목표: 편의성·가시성 개선 / 카드 설명 상세화 / 덱 빌딩 UI 씬 분리

---

## 1. 지금 뭐가 문제인지 (코드 기준)

| 증상 | 원인 |
|---|---|
| 카드 찾기가 고통 | 41종을 **300×385px 안에 2열 그리드** — `DeckBuildingController.cs:98`, `:124`. 21행 스크롤, 필터·검색·정렬 전부 없음 |
| 설명이 안 읽힘 | 설명이 132×198 썸네일 안쪽 `DesBox`(카드 폭의 62%)에 들어감 — `CardView.cs:20`. `resizeTextMinSize = 4` 라 한글 3~4줄이면 **4px까지 줄어듦** (`CardView.cs:133`) |
| 눌러도 반응이 없음 | `AddCard`가 3장 초과·15장 초과면 **조용히 return**. 컬렉션 카드에 보유 수 표시가 없어 이유를 알 수 없음 |
| 덱 구성이 안 보임 | 덱 목록이 `"이름 x3"` 텍스트 한 줄뿐. 코스트·속성·커브 정보 0 |

---

## 2. 편의성 / 가시성 — 효과 큰 순서

1. **속성 탭 필터** (전체/화염/얼음/바람/번개/회복/드로우/질주/토템/특수)
   `CardDef.Attribute`가 이미 있으니 필터 한 줄이면 41종 → 5종씩. 스크롤 문제의 90%가 사라짐. **제일 싸고 제일 큼.**
2. **컬렉션 카드에 보유 수 뱃지** (`2/3`) + 3장 차면 어둡게 → "반응 없음" 문제 해결
3. **상세 패널 1장** — 카드 클릭/호버 시 큰 카드 + 전체 설명.
   카드 썸네일 안의 4px 설명은 **아예 지우고**(`DeckBuildingController.cs:165`) 상세 패널에서만 표시. 이게 "설명 자세히"의 본체.
4. **덱 목록 행 개선** — 속성 컬러 바 + 코스트 뱃지 + `x수량`. 클릭=1장 제거는 유지
5. **코스트 커브 미니 바** — 15장이라 가로 막대 한 줄로 충분. "덱이 무겁다"가 한눈에
6. **비우기 / 랜덤 채우기 버튼** — 잼 시연·테스트 체감 큼
7. 정렬(코스트/티어) — 1번 하고 나면 후순위

---

## 3. 카드 설명 자세히

`CardText.Descriptions`는 이미 충분히 자세하다. **문제는 내용이 아니라 자리.**
상세 패널이 생기면 그 텍스트를 원본 그대로 쓰고, `CardDef`에 이미 있는 값을 칩으로 덧붙인다:

- 코스트 / 사거리 / 위력 / 티어 → 숫자 줄
- `Arc` → `장애물 관통` vs `장애물에 막힘`
- `AllowEmptyTile` → `빈 칸 조준 가능`
- `TargetKind` → `적 지정` / `타일 지정` / `방향` / `자신`

`GetDescription`은 지금 `{0}=Range {1}=Power`만 치환한다. 새 문구는 `CardText`에만 넣을 것 — GameUI가 같은 소스를 보므로 인게임 툴팁도 같이 좋아진다.

---

## 4. 씬 분리 — 실제로 건드려야 하는 것 4개

1. **`DeckScene.unity` 신규** + `DeckBuildingController`가 `transform.Find("MenuCanvas/DeckUI")` 의존을 버리고 자기 Canvas를 직접 생성
   → 화면 전체를 쓰므로 위 300px 문제의 **근본 원인**이 사라짐
2. **매칭 결합 끊기** — `MenuMatchmakingController.cs:42`의 `_deckBuilder.CurrentDeck` → `LocalPrefs.Deck`
   덱은 이미 편집할 때마다 저장되므로 이 한 줄이면 끝. `GetComponent<DeckBuildingController>()`도 삭제
3. **⚠️ 인트로 재생 함정** — 덱 씬에서 돌아오면 `IntroRegistrationController.Awake`가 `TitleState`부터 다시 켠다 (`IntroRegistrationController.cs:52`).
   타이틀 → StartButton → 편지 애니 → 로비를 매번 다시 봐야 함. **"로비부터 시작" 플래그 하나**(static bool로 충분) 필요
4. **Build Settings에 DeckScene 추가** — 빠지면 `LoadScene` 런타임 에러

> 대안: 씬을 나누지 않고 `DeckUI/Panel`을 풀스크린으로 키우기만 해도 가시성 문제는 거의 해결되고 3·4번 함정이 없다.

---

## 5. 제안 순서

```
1. 씬 분리(풀스크린 확보) → 확인: 덱 씬 진입/복귀 시 타이틀 안 나오고, 매칭이 저장된 덱으로 붙음
2. 속성 필터 + 보유 수 뱃지  → 확인: 탭 눌러 5장씩, 3장 채운 카드가 흐려짐
3. 상세 패널(설명 이전)      → 확인: 카드 안 4px 설명 제거, 패널에서 전문 읽힘
4. 커브 바 / 비우기·랜덤     → 확인: 15/15 채우고 커브가 맞게 그려짐
```
